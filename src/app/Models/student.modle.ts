export interface StudnetModel {
  stId: number;
  Name: string;
  email: string;
  phone: string;
}

export interface StudnetBackEndModel {
  id: number;
  name: string;
  email: string;
  phoneNumber: string;
  city: string;
}
